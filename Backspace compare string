class Solution:
    def backspaceCompare(self, s: str, t: str) -> bool:
        def process(string):
            stack=[]
            for i in string:
                if i=='#':
                    if stack:
                        stack.pop()
                else:
                    stack.append(i)
            return stack
        return process(s)==process(t)

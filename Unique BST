class Solution:
    def numTrees(self, n: int) -> int:
        d = [0] * (n + 1)
        d[0] = 1
        d[1] = 1

        for nodes in range(2, n + 1):
            for root in range(1, nodes + 1):
                d[nodes] += d[root - 1] * d[nodes - root]

        return d[n]
